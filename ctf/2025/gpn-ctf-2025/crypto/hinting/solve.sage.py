

# This file was *autogenerated* from the file solve.sage
from sage.all_cmdline import *   # import sage library

_sage_const_0 = Integer(0); _sage_const_7 = Integer(7); _sage_const_2 = Integer(2); _sage_const_500 = Integer(500); _sage_const_4 = Integer(4)
with open("output.txt", "r") as f:
    exec(f.read())

A = _sage_const_0 
for i, v in enumerate(V):
    A += v * (_sage_const_7 **i)

n = Integer(n)
S_approx = _sage_const_2  * isqrt(n)
B_approx = (S_approx - A) // _sage_const_7 

print(f"[*] n = {n}")
print(f"[*] V = {V}")
print(f"[*] A = {A}")
print(f"[*] S_approx = {S_approx}")
print(f"[*] B_approx = {B_approx}")

for i in range(-_sage_const_500 , _sage_const_500 ):
    B_cand = B_approx + i
    S_cand = A + _sage_const_7  * B_cand
    
    delta_sq = S_cand**_sage_const_2  - _sage_const_4 *n
    if delta_sq > _sage_const_0  and is_square(delta_sq):
        print(f"\n[+] Found a perfect square for i = {i}!")
        delta = isqrt(delta_sq)
        p = (S_cand + delta) // _sage_const_2 
        q = (S_cand - delta) // _sage_const_2 
        
        if p*q == n:
            print(f"[+] Found p and q!")
            print(f"p = {p}")
            print(f"q = {q}")
            
            # Sanity check with base-7 digits
            pp_digits = p.digits(base=_sage_const_7 )
            qq_digits = q.digits(base=_sage_const_7 )
            
            match = True
            for i in range(len(V)):
                pi = pp_digits[i] if i < len(pp_digits) else _sage_const_0 
                qi = qq_digits[i] if i < len(qq_digits) else _sage_const_0 
                if (pi + qi) % _sage_const_7  != V[i]:
                    match = False
                    break
            if match:
                print("[+] Base-7 digits hint matches.")
            else:
                print("[-] Base-7 digits hint does not match. Continuing search.")
                continue

            break
        else:
            print(f"[-] p*q != n for i = {i}") 

